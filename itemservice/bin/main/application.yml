# Bootstrap configuration only - all other settings come from Config Server
spring:
  application:
    name: item-service
  config:
    import: configserver:${CONFIG_SERVER_URL:http://localhost:8888}
  cloud:
    config:
      fail-fast: true
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}