# Docker Compose for local development - Only infrastructure services
# Use this when you want to run microservices from IDE but need PostgreSQL

# version: '3.8'

services:
  # PostgreSQL Database
  postgres:
    image: postgres:16-alpine
    container_name: e4u-postgres
    environment:
      POSTGRES_USER: nam
      POSTGRES_PASSWORD: nam123
      POSTGRES_MULTIPLE_DATABASES: e4u_auth,e4u_account,e4u_item,e4u_statistic,e4u_payment,e4u_curriculum,e4u_lesson
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./docker/postgres/init-multiple-databases.sh:/docker-entrypoint-initdb.d/init-multiple-databases.sh
    ports:
      - "5432:5432"
    networks:
      - e4u-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nam"]
      interval: 10s
      timeout: 5s
      retries: 5

  # pgAdmin - PostgreSQL Web UI (optional)
  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: e4u-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@e4u.com
      PGADMIN_DEFAULT_PASSWORD: admin123
    ports:
      - "5050:80"
    networks:
      - e4u-network
    depends_on:
      - postgres

  # Redis Cache (for curriculum-service and lesson-service)
  redis:
    image: redis:7-alpine
    container_name: e4u-redis
    ports:
      - "6379:6379"
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    volumes:
      - redis_data:/data
    networks:
      - e4u-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

networks:
  e4u-network:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
